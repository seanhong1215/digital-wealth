{
  // ============================================================================
  // tsconfig.json（根目錄）— 整個 monorepo 的型別檢查進入點
  //
  // 這個檔案是什麼：
  //   它本身不編譯任何程式碼（"files": [] 就是這個意思），
  //   只是列出所有子專案，讓 `tsc --build` 知道要依序檢查哪些。
  //
  // 為什麼需要它：
  //   在根目錄執行 `npm run typecheck` 時，tsc 需要一個進入點。
  //   沒有這個檔案的話，你得手動一個一個下 `tsc -p shared`、`tsc -p api`...
  //
  // 這叫 Project References（專案參照）：
  //   TypeScript 會依照 references 的順序建置，並且只重新編譯有變動的部分。
  //   在 monorepo 裡這能省下大量的重複編譯時間。
  //
  // 【後續單元記得回來加】
  //   api         → 單元 0.6
  //   web         → 單元 1.2
  //   market-feed → 單元 2.1
  // ============================================================================

  "files": [],
  "references": [
    { "path": "./shared" }
  ]
}
