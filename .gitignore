# ==============================================================================
# .gitignore — 告訴 git 哪些檔案不要納入版控
#
# 判斷原則：
#   1. 可以重新產生的（node_modules、dist、build 產物）→ 不進版控
#   2. 含機密的（.env、金鑰）→ 不進版控
#   3. 個人環境專屬的（編輯器設定、OS 產生的檔案）→ 不進版控
# ==============================================================================

# ------------------------------------------------------------------------------
# 相依套件 —— 可由 lockfile 重新安裝，且體積龐大
# ------------------------------------------------------------------------------
node_modules/
.pnp/
.pnp.js

# ------------------------------------------------------------------------------
# 環境變數 ★ 最重要的一條
#
# .env 可能含有密碼與金鑰，絕對不能進版控。
# 但 .env.example 要進版控（用 ! 反向排除），因為那是給別人看的規格。
# ------------------------------------------------------------------------------
.env
.env.local
.env.*.local
!.env.example

# ------------------------------------------------------------------------------
# 建置產物 —— 每次 build 都會重新產生
# ------------------------------------------------------------------------------
dist/
build/
out/
*.tsbuildinfo

# Vite 的快取目錄
.vite/
node_modules/.vite/

# NestJS 編譯輸出
api/dist/

# ------------------------------------------------------------------------------
# 測試與覆蓋率報告
# ------------------------------------------------------------------------------
coverage/
.nyc_output/
playwright-report/
test-results/

# ------------------------------------------------------------------------------
# 日誌
# ------------------------------------------------------------------------------
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
logs/

# ------------------------------------------------------------------------------
# 編輯器與 IDE
#
# .vscode/ 部分設定其實值得共用（如推薦擴充套件），
# 所以只排除個人化的部分，保留 extensions.json 與 settings.json。
# ------------------------------------------------------------------------------
.idea/
*.swp
*.swo
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json

# ------------------------------------------------------------------------------
# 作業系統產生的檔案
# ------------------------------------------------------------------------------
.DS_Store
Thumbs.db
desktop.ini

# ------------------------------------------------------------------------------
# 本專案特有
# ------------------------------------------------------------------------------

# 資料庫備份檔（若之後有匯出 dump 的需求）
*.dump
*.sql.gz

# demo 影片的原始素材（成品才進版控）
docs/screenshots/raw/
